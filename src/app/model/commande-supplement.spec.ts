import { CommandeSupplement } from './commande-supplement';

describe('CommandeSupplement', () => {
  it('should create an instance', () => {
    expect(new CommandeSupplement()).toBeTruthy();
  });
});
