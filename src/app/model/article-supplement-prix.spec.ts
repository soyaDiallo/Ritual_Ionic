import { ArticleSupplementPrix } from './article-supplement-prix';

describe('ArticleSupplementPrix', () => {
  it('should create an instance', () => {
    expect(new ArticleSupplementPrix()).toBeTruthy();
  });
});
